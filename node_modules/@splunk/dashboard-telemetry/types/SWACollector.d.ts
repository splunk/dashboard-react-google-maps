import { MetricsCollector, CollectorPayload } from './MetricsCollector';
declare type pusherFunction = (event: CollectorPayload) => void;
declare global {
    interface Window {
        _splunk_metrics_events?: {
            push: pusherFunction;
        };
    }
}
export declare const NOT_REGISTERED_WARNING = "SWACollector was initialized, but SWA does not appear to be configured on the page.";
export declare class SWACollector implements MetricsCollector {
    constructor();
    /**
     * Sends metrics data to a SWA metrics service
     * @param payload Processed data from the telemetry API
     */
    sendEvent(payload: CollectorPayload): void;
}
export {};
//# sourceMappingURL=SWACollector.d.ts.map