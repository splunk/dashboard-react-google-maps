import { SearchMetric } from './filters/filterSearchMetrics';
/**
 * This is the expected payload for an "emit" api event
 */
export declare type EmittableEvent = {
    pageAction?: string;
    udfVersion?: string;
    definition?: Record<string, unknown>;
    definitionEdits?: Record<string, unknown>;
    metadata?: Record<string, unknown>;
    loadTime?: string | null;
    error?: string;
    searchMetrics?: SearchMetric[];
    source?: string;
    event?: string;
    undoActions?: string[];
    redoActions?: string[];
    [key: string]: unknown;
};
/**
 * This is the expected payload for a "collect" api event
 */
export declare type CountableEvent = {
    source: 'canvas' | 'keyboard' | 'actionMenu';
    event: string;
    metadata?: Record<string, unknown>;
};
//# sourceMappingURL=EventTypes.d.ts.map