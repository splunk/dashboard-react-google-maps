/**
 * check if a url starts with http or https
 * @param {String} url
 */
export declare const isAbsoluteURL: (url: string) => boolean;
/**
 * check if a url is a valid relative url
 * @param {String} url
 */
export declare const isRelativeURL: (url: string) => boolean;
/**
 * check if a url is consider as valid
 * @param {String} url
 */
export declare const isValidUrl: (url: string) => boolean;
export declare const getWindowLocationSearch: () => string;
//# sourceMappingURL=url.d.ts.map