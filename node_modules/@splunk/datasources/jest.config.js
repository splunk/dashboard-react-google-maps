const buildConfig = require('@splunk/dashboard-build-tools/jest-config')
    .default;

const configs = buildConfig({
    react: false,
    suiteName: 'datasources',
});

module.exports = {
    ...configs,
    // need extra setup for splunk enterprise
    setupFiles: [...configs.setupFiles, './jestSetupEnterprise.config.js'],
};
