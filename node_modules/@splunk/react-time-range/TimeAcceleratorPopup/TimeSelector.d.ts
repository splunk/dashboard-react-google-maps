import { FC } from 'react';
export interface TimeSelectorProps {
    /**
     * Handler for time change event
     */
    onTimeChanged: (data: {
        earliest: string;
        latest: string;
    }) => void;
    /**
     * Timestamp of event selected
     */
    eventTime: string;
    /**
     * Array of time units to be added to a menu
     */
    timeUnits?: TimeUnit[];
    /**
     * The default selected time unit
     */
    timeUnit?: TimeUnit;
}
export declare enum TimeUnit {
    Week = "w",
    Day = "d",
    Hour = "h",
    Minute = "m",
    Second = "s",
    Millisecond = "ms"
}
declare const TimeSelector: FC<TimeSelectorProps>;
export default TimeSelector;
