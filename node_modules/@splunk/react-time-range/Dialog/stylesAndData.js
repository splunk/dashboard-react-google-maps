"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _lodash = require("lodash");

const styleOrData = extra => (value, key) => {
  switch (key) {
    case 'style':
    case 'className':
      return true;

    default:
      if (extra && extra.has(key)) {
        return true;
      }

      return key.startsWith('data-') || key.startsWith('aria-');
  }
};

const stylesAndData = (props, extra) => (0, _lodash.pickBy)(props, styleOrData(extra));

var _default = stylesAndData;
exports.default = _default;