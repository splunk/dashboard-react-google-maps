import { ReactElement } from 'react';
import { EventHandler } from './Panel';
interface PresetLinkProps {
    children: string;
    earliest: string;
    latest: string;
    label?: string;
    onClick: EventHandler;
}
declare function PresetLink({ children, earliest, latest, label, onClick, ...rest }: PresetLinkProps): ReactElement;
export default PresetLink;
