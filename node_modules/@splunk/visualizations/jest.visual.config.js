const buildConfig = require('@splunk/visualization-build-tools/jest-config').default;

module.exports = buildConfig(
    {
        react: true,
        coverageKind: 'visual',
        suiteName: 'visualization',
        browser: false,
    },
    {
        // visual tests should not generate code coverage
        coveragePathIgnorePatterns: ['/node_modules/'],
    }
);
