import type { SetTokenEvent } from '@splunk/dashboard-types';
interface HandlePayload {
    tokens: Record<string, string>;
    tokenNamespace?: string;
    submit?: boolean;
}
declare class DefaultInputHandler {
    canHandle({ type }: {
        type?: string;
    }): boolean;
    handle({ payload }: {
        payload: HandlePayload;
    }): Promise<[SetTokenEvent]>;
}
export default DefaultInputHandler;
//# sourceMappingURL=DefaultInputHandler.d.ts.map